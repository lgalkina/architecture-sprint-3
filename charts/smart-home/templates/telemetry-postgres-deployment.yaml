apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemetryPostgres
spec:
  selector:
    matchLabels:
      app: telemetryPostgres
  replicas: 1
  template:
    metadata:
      labels:
        app: telemetryPostgres
    spec:
      containers:
        - name: telemetryPostgres
          image: {{ .Values.devicePostgres.image }}
          ports:
            - containerPort: {{ .Values.devicePostgres.port }}
          env:
            - name: POSTGRES_DB
              value: {{ .Values.devicePostgres.database }}
            - name: POSTGRES_USER
              value: {{ .Values.devicePostgres.username }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.devicePostgres.password }}