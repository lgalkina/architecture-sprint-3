apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemetry-postgres
spec:
  selector:
    matchLabels:
      app: telemetry-postgres
  replicas: 1
  template:
    metadata:
      labels:
        app: telemetry-postgres
    spec:
      containers:
        - name: telemetry-postgres
          image: {{ .Values.telemetryPostgres.image }}
          ports:
            - containerPort: {{ .Values.telemetryPostgres.port }}
          env:
            - name: POSTGRES_DB
              value: {{ .Values.telemetryPostgres.database }}
            - name: POSTGRES_USER
              value: {{ .Values.telemetryPostgres.username }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.telemetryPostgres.password }}